import { styled } from '@mui/material/styles';
import { FormHelperText, TextField } from '@mui/material';

export const MuiTextField = styled(TextField)`
  width: 100%;
`;

export const MuiFormHelperText = styled(FormHelperText)`
  margin: 8rem 0 0;
`;
